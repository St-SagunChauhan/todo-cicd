import { makeStyles } from '@mui/styles';

const useCustomDataGridStyles = makeStyles(() => ({
  // root: {
  //   backgroundColor: '#fff',
  //   borderRadius: '10px',
  // },

  dropBack: {
    backgroundColor: '#fff',
    borderRadius: '5px',
  },
}));

export default useCustomDataGridStyles;
